let names = ['Nancy','Blessing','Jorge','Svetlana','Bob']

// let results = names.filter((word) => nam.charAt (0) === 'b')

// let namesLength = nam.map((name) = names.length)

let reduce = names.reduce((names) => total + names.length, 0) / names.length; 
